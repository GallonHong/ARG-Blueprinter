{
  "name": "arg-blueprint",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "license": "MPL-2.0",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "bridge": "node plugins/dsh-arg-plugin/bridge-server.js",
    "mcp": "node plugins/dsh-arg-plugin/mcp-server.js",
    "test": "node --test tests/*.test.mjs",
    "audit:templates": "node --test tests/audit_templates.mjs",
    "test:self-play": "node --test tests/self_play_full_game.mjs",
    "test:all": "npm test && npm run audit:templates && npm run test:self-play"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "@vitejs/plugin-react": "latest",
    "jszip": "^3.10.1",
    "react": "latest",
    "react-dom": "latest",
    "vite": "latest",
    "zod": "^4.4.3"
  }
}
